export enum StreamStatus {
    Started = "started",
    Stopped = "stopped"
}

export enum LogType {
    Click = "click",
    View = "view"
}

export enum StreamType {
    Live = 'Live',
    VOD = 'VOD',   // Video on demand
    // Add more types if needed, e.g. 'Archived', 'Scheduled'
}
